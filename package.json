{
  "name": "superta",
  "version": "0.1.0",
  "private": true,
  "description": "Gmail-backed OpenClaw teaching delegate scaffold",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node dist/evals/run-evals.js",
    "lint": "echo 'lint scaffold pending'"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}
